# =============================================================================
# Secret — Quantum Options Pricer
# =============================================================================
# Do NOT commit real values. Create the secret from your .env file:
#
#   kubectl -n quantum-pricer create secret generic quantum-pricer-secret \
#     --from-env-file=.env \
#     --dry-run=client -o yaml | kubectl apply -f -
#
# Or create manually:
#
#   kubectl -n quantum-pricer create secret generic quantum-pricer-secret \
#     --from-literal=DATABENTO_API_KEY=<your-key>
#
# The Deployment mounts this secret as env vars via envFrom.
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: quantum-pricer-secret
  namespace: quantum-pricer
type: Opaque
stringData:
  DATABENTO_API_KEY: "REPLACE_ME"
