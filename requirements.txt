# =============================================================================
# Quantum Options Pricer — Python Dependencies
# Pinned for: Qiskit 2.3 + CUDA 12.4.1 + NVIDIA T4/L4
# =============================================================================

# ── Quantum Core ──────────────────────────────────────────────────────────────
qiskit==2.3.0
qiskit-aer==0.17.2               # CPU wheel — overridden by CUDA source build in Dockerfile
qiskit-finance==0.4.1            # LogNormalDistribution, EuropeanCallPricing
qiskit-algorithms==0.4.0         # IterativeAmplitudeEstimation (V2-primitive-aware)
cuquantum-python-cu12            # NVIDIA cuTensorNet bindings (tensor_network method)

# ── API Layer ────────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.7.0
pydantic-settings==2.3.0
scalar-fastapi>=1.6.0             # Scalar interactive API docs at /docs

# ── Logging ──────────────────────────────────────────────────────────────────
python-json-logger==2.0.7
PyYAML==6.0.1

# ── Market Data ──────────────────────────────────────────────────────────────
databento>=0.71.0               # OPRA.PILLAR NBBO snapshots (Pay-As-You-Go)
yfinance>=0.2.0                 # Spot prices (ticker) + risk-free rate (^IRX T-bill)

# ── Numerics ────────────────────────────────────────────────────────────────
numpy>=1.26.0
scipy>=1.12.0                   # BS IV inversion (brentq) + norm.cdf

# ── System ───────────────────────────────────────────────────────────────────
tzdata>=2024.1                  # zoneinfo("America/New_York") on CUDA slim images
